/** Official brand logo (served from public/). Used by the sidebar, login,
 *  public portal and printed reports. Admins may override the sidebar mark via
 *  the "Municipality Logo" setting; this is the default. */
export const BRAND_LOGO = "/logo/alaminos-seal.png";
